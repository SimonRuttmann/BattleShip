package Controller.Handler;

public class ControlThreadGetShot extends Thread{
    @Override
    public void run() {


        //TODO WHILE TRUE (OR IS RUNNING) mit gegner beschiessen beschossen, abbruch mit den Befehlen speichern, abbruch, win/lose oder timeout <- Gehört woanders hin
        //Reaction es wird am ende eine methode aufgerufen, die REAKTION heist, welche auf den behehl wartet getCMD()


        //alle felder auf disabled setzten
        //neuer Thread
        //getCMD()
        // -> Playground .idsfjslff
        // der setzt dann die Labels usw -> yourTurn true
        // yourTurn = true alle label auf nicht disabled setzten (also nur wasser felder)

        //TODO in beiden Methoden, jegliche andere Befehle save/abbruch/... abfangen
        //TODO Es werden NUR SYNTAKTISCH RICHTIGE Befehle übergeben
    }
}
