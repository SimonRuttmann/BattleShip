package Player;
//Not in use
import Model.Util.IDrawable;
import Model.Util.ShipPart;
import Model.Util.ShotWater;
import Model.Util.UtilDataType.Point;
import Model.Util.Water;

public class InterfaceDrawableAdapter {
   /* Point position;
    ShotWater shotWater;
    Water water;
    ShipPart shipPart;
    int i;

    public InterfaceDrawableAdapter(IDrawable drawable){
        if ( drawable instanceof ShotWater){
            this.shotWater = (ShotWater)drawable;
            this.i = 0;
        }
        else if (drawable instanceof Water){
            this.water = (Water)drawable;
            this.i = 1;
        }
        else {
            this.shipPart = (ShipPart)drawable;
            this.i = 2;
        }

    }*/
}
